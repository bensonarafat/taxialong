import 'package:flutter/material.dart';

class TaxiAlongErrorWidget extends StatelessWidget {
  const TaxiAlongErrorWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Error"));
  }
}
