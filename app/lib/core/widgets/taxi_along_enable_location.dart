import 'package:flutter/material.dart';

class TaxiAlongEnableLocation extends StatelessWidget {
  const TaxiAlongEnableLocation({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
